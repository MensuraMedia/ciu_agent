# CC Token Saver MCP

Placeholder for the local LLM delegation server. This MCP offloads simple tasks
(docstrings, boilerplate, commit messages) to a local model (e.g., Qwen 2.5-7B
via LM Studio) to reduce Claude API token usage.

See `docs/token_ops.md` for delegation rules.

## Status

Not yet implemented. Optional dependency — only needed when a local LLM is available.
